	{exp:weblog:entries status="not Closed"}
		
		{if general_banner != ""}
		<img src="{general_banner}" alt="{title}" />
		{/if}

		<h1>{title}</h1>
					
		{general_content}			

	{/exp:weblog:entries}